@model DateTime?

@{

    DateTime dateTime = DateTime.Now;

    if (Model != null)

    {

        dateTime = (System.DateTime)Model;

    }

    @Html.TextBox("", String.Format("{0:d}", dateTime.ToShortDateString()), new { @class = "form-control datecontrol", type = "date" })

}