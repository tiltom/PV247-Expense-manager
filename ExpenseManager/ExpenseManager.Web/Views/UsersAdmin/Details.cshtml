@using ExpenseManager.Resources.UsersAdminResources
@model ExpenseManager.Web.Models.User.UserDetailViewModel

@{
    ViewBag.Title = UsersAdminResource.UserDetails;
}

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UserName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>
    </dl>
</div>

<div class="panel panel-default">
    <div class="panel-heading">
        <h4>
            @UsersAdminResource.ListOfRoles
            <span class="badge">@Model.RolesList.Count()</span>
        </h4>
    </div>

    <ul class="list-group">
        @if (!Model.RolesList.Any())
        {
            <li class="list-group-item">
                @UsersAdminResource.NoRoles
            </li>
        }
        @foreach (var item in Model.RolesList)
        {
            <li class="list-group-item">
                @item
            </li>
        }
    </ul>
</div>

    <p>
        @Html.ActionLink(UsersAdminResource.EditUser, "Edit", new { id = Model.Id }, new { @class = "btn btn-primary" })
        @Html.ActionLink(SharedResource.Cancel, "Index", null, new { @class = "btn btn-danger" })
    </p>



